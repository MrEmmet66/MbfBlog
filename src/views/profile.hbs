<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #0f0f0f;
            color: #ffffff;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 1000px;
            margin: 50px auto;
            background-color: #0e0d0f;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            padding: 20px;
        }

        .profile {
            display: flex;
            align-items: flex-start;
            padding: 20px;
        }

        .profile-info {
            flex: 1;
        }

        img.avatar {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            margin-bottom: 10px;
            position: absolute;
            top: 30px;
            left: 30px;
            border: 4px solid white;
        }

        img.banner {
            width: 100%;
            border-radius: 10px;
            margin-bottom: 15px;
        }

        h2 {
            color: #2e86de;
        }

        p {
            color: #b3b3b3;
            margin-bottom: 10px;
        }

        .post {
            border: 3px solid #67368f;
            padding: 10px;
            margin-top: 10px;
            background-color: #18191a;
        }

        .post-user {
            display: flex;
            align-items: center;
        }

        .post-user img {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            margin-right: 10px;
        }

        .post-content {
            margin-top: 10px;
        }

        .post-date {
            color: #999999;
            font-size: 12px;
        }

        /* Общие стили */
        ::-webkit-scrollbar {
            width: 10px; /* Ширина вертикального скроллбара */
        }

        ::-webkit-scrollbar-track {
            background-color: #333333; /* Цвет фона скроллбара */
        }

        ::-webkit-scrollbar-thumb {
            background-color: #8A2BE2; /* Цвет ползунка скроллбара */
            border-radius: 5px; /* Скругление углов ползунка */
        }

        /* Операция скроллбара при наведении курсора */
        ::-webkit-scrollbar-thumb:hover {
            background-color: #BB86FC; /* Измененный цвет ползунка при наведении */
        }
        
    </style>
</head>
<body>
    <div class="container">
        <h1>User Profile</h1>
        <div class="profile">
            <div class="profile-info">
                <img class="avatar" src="{{avatar}}" alt="Avatar">
                <img class="banner" src="https://png.pngtree.com/background/20210712/original/pngtree-hand-drawn-cartoon-cute-anime-banner-background-picture-image_1187044.jpg" alt="Banner">
                <h2>{{username}}</h2>
                <p>Number of Followers: {{followers}}</p>
            </div>
        </div>

        <div id="posts-container">
            {{#each posts}}
            <div class="post">
                <div class="post-user">
                    <img class="post-avatar" src="https://steamuserimages-a.akamaihd.net/ugc/1664610068472386993/AEFFCA6FA72E4F5FB2C80A78BE368EA641912EAC/?imw=512&&ima=fit&impolicy=Letterbox&imcolor=%23000000&letterbox=false" alt="User Avatar">
                    <p>{{name}}</p>
                </div>
                <div class="post-content">
                    <p>{{content}}</p>
                </div>
                <div class="post-date">
                    <p>{{date}}</p>
                </div>
            </div>
            {{/each}}
        </div>
    </div>
</body>
</html>